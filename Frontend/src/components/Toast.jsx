// Toast is handled inline in App.jsx via the toast-wrap / toast CSS classes.
// This file is intentionally empty — the export is a no-op placeholder
// so older imports don't break.
export default function Toast() { return null }
